1. Check for token being an int (we want to leave out years and tokens that are meaningless)

2. (Optional) Add implementation for parsing the robots.txt file
    - the function should be done, just connect to our crawler and make sure it works

3. (Optional) Add implementation for parsing the sitemaps file and adding them to the unique counts
    - the function should be done, just connect to our crawler and make sure it works

4. (Optional) Add implementation for the crawl delay on specific websites
    - the function should be done, just connect to our crawler and make sure it works
        - if the website does not specify a crawl delay limit, we just leave it at the standard
